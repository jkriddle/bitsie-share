<p>Thank you for using Bitsie Share! Simply share the link below with your customers in order to begin accepting payments for access to the file.</p>

<p><a style="font-size:16px;" href="{{url}}">{{url}}</a></p>

<p>Your file will be available for <strong>30 days</strong> after the last purchase made. If you would like to keep your file active and have not had any recent purchases, simply purchase it for yourself before it expires.</p>

<p><strong>Receipt</strong></p>

<table class="table table-striped table-bordered">
	<tbody>
		<tr>
			<td><strong>Upload ID</strong></td>
			<td>{{upload.slug}}</td>
		</tr>
		<tr>
			<td><strong>Share URL</strong></td>
			<td><a href="{{url}}">{{url}}</a></td>
		</tr>
		<tr>
			<td><strong>Title</strong></td>
			<td>{{upload.title}}</td>
		</tr>
		<tr>
			<td><strong>File Name</strong></td>
			<td>{{upload.filename}}</td>
		</tr>
		<tr>
			<td><strong>File Size</strong></td>
			<td>{{upload.filesize}} bytes</td>
		</tr>
		{{#if upload.description}}
			<tr>
				<td style="vertical-align:top;"><strong>Description</strong></td>
				<td>{{upload.description}}</td>
			</tr>
		{{/if}}
		{{#if upload.authorName}}
			<tr>
				<td><strong>Author Name</strong></td>
				<td>{{upload.authorName}}</td>
			</tr>
		{{/if}}
		<tr>
			<td><strong>Payment Address</strong></td>
			<td>{{upload.payoutAddress}}</td>
		</tr>
		<tr>
			<td><strong>Customer Price</strong></td>
			<td>${{price}}</td>
		</tr>
		<tr>
			<td><strong>Upload Date</strong></td>
			<td>{{upload.createdAtString}}</td>
		</tr>
	</tbody>
</table>